#include <iostream>
#include <string>

bool check(int x) {
	std::string s = std::to_string(x);
	std::string r = s;
	std::reverse(r.begin(), r.end());
	return s == r;
}

int 

int main(int argc, char *argv[]) {  
	
}